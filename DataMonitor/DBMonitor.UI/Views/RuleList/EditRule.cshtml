@model DBMonitor.UI.Models.RuleViewModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-md-4">
        <form asp-action="EditRule" asp-controller="RuleList" method="post" asp-antiforgery=false asp-route-id=@Model.Id class="form-horizontal">
            @Html.EditorFor(x => x, new {@Title="Редактирование правила"})
        </form>
    </div>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
    await Html.RenderPartialAsync("_ValidationScriptsPartial");
}
}
